How to use:
1.在users目录下创建用户文件夹，并将编译好的.class文件放入其中。
2.在input中构造测试样例，每条以换行符结尾。
3.打开bash，cd进入test目录下，键入./autoTest.sh 用户名 程序入口的.class文件名，如./autoTest.sh my Main（.class后缀不需要输入）。

Tips:
1.确保系统装有JVM和Python3.
2.仅支持合法数据的测试。
